<div class="container">
  <!-- Title -->
  <h2>How active will business be in {{checkintime}} hour value</h2>
  <br>

  <!-- Output -->
  <table class="table table-bordered">
    <thead class="thead-dark">
      <tr>
        <th scope="col">Check-In Time</th>

      </tr>
    </thead>
    <tbody>
      {{#if result}}
      {{#each result}}
      <tr>
        <td>{{this.BusyChance}}</td>

      </tr>
      {{/each}}
      {{else}}
      <tr>
        <td>No value.</td>
      </tr>
      {{/if}}
    </tbody>
  </table>

  <!-- Buttons -->
  <div class="btnGroup">
    <button class="btn btn-outline-info leftbtn" id="again-btn">Enter New Input</button>
    <button class="btn btn-outline-secondary rightbtn" id="home-btn">Back to Home</button>
  </div>
</div>